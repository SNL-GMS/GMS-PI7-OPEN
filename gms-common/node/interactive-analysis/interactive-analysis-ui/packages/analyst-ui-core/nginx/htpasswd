#
# Temporary demo password file used by Nginx to control access to the
# interactive analysis UI
#
# Each `<USERNAME>:<HASHED_PASSWORD>` line specifies one user.  The password hashes
# can be generated by running `openssl passwd -apr1`.
#

# Demo user gms-user
gms-user:$apr1$/IyAA20F$YMr2FBgief5q2zaJZGa4y1
